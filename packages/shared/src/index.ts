/**
 * Placeholder entry point for `packages/shared`.
 *
 * This package will host the pure game-logic modules (`crossword-generator`,
 * `word-assignment`, `word-list-validator`, `guess-checker`) added by later
 * issues (see PRD — GitHub issue #1). For now it exists only to prove the
 * TypeScript build and Vitest pipeline work end-to-end for this workspace.
 */
export const packageName = 'shared';
